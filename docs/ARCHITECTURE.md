# Backend Architecture Pointer

See shared architecture guide:
- `../docs/ARCHITECTURE.md` (in monorepo)

When backend is moved to a standalone repo, copy that file into this repo's `docs/` folder as `ARCHITECTURE.md`.
